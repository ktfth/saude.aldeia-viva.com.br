__pycache__
*.py[cod]
.venv
venv
.env
.git
*.md
.dockerignore
Dockerfile
docker-compose*.yml
.cursor

# Credenciais nao entram no artefato. Uma imagem construida na maquina de
# quem tem `data/users.json` carregava a chave premium para dentro dela --
# verificado construindo e inspecionando a imagem. Passe as chaves por
# `USERS_DB_JSON` ou monte o arquivo em tempo de execucao.
data/users.json

# Cache local de execucao. Sem isto a imagem herdava os 42 MB da maquina de
# quem construiu, e o container carregava o relatorio DAQUELE cache -- a
# imagem deixava de ser reproduzivel. Verificado no log do container: sem o
# cache, ele sobe por `bundled_report_snapshot.py`.
#
# Correcao de uma afirmacao errada que estava aqui: a versao anterior deste
# comentario dizia que o fallback era `data/reports`. Nao era. Aquele
# diretorio nao era lido por codigo nenhum, e foi removido.
.cache
data/usage.jsonl

# Nao sao executados em producao.
tests
docs
.ralph
.pi-lens
