.venv/
venv/
__pycache__/
*.py[cod]
.env
.pytest_cache/
.vercel
.cache/

# Saída de execução local — telemetria de uso, cresce a cada requisição.
data/usage.jsonl

# Caches de ferramentas locais.
.ralph/
.pi-lens/

# Override de composição local, por máquina.
docker-compose.override.yml

# Saida de execucao de testes de navegador.
test-results/

# Chaves de API. Mantido FORA do versionamento de proposito: o arquivo guarda
# credenciais, e a decisao de publica-las ou rotaciona-las e de quem opera o
# servico. Um `git add -A` o levou junto uma vez; esta regra impede a
# repeticao.
data/users.json
